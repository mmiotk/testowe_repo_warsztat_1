# Workflow dla zmian w kodzie backendowym
name: Backend Tests
on:
  push:
    paths:
      - 'server/**'           # Wszystko w katalogu server
      - 'api/**'              # Wszystko w katalogu api
      - 'database/**'         # Zmiany w bazie danych
      - 'package.json'        # Zmiany w zależnościach

jobs:
  test_backend:
    runs-on: ubuntu-latest
    steps:
      - name: Uruchom testy backendu
        run: |
          echo "Wykryto zmiany w backendzie, uruchamianie testów..."
          echo "Sprawdzanie API, bazy danych i logiki biznesowej"
