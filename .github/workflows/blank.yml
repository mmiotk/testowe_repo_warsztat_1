on:
  push:

jobs:
  debug_event:
    runs-on: ubuntu-latest
    steps:
      - name: Wyświetl pełny kontekst zdarzenia
        env:
          EVENT_CONTEXT: ${{ toJSON(github.event) }}
        run: |
          echo "📊 Pełne informacje o zdarzeniu:"
          echo "$EVENT_CONTEXT" | jq '.' || echo "$EVENT_CONTEXT"

      - name: Wyświetl podstawowe informacje o GitHub
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Event name: ${{ github.event_name }}"
          echo "Actor: ${{ github.actor }}"
          echo "Ref: ${{ github.ref }}"
          echo "SHA: ${{ github.sha }}"
