#!/bin/bash
set -e # Zatrzymaj skrypt przy pierwszym błędzie

echo "Minimal setup: Przygotowywanie środowiska..."
# Tutaj mogłaby być np. instalacja jakiegoś małego narzędzia,
# ale dla minimalizmu zostawiamy tylko echo.
# Przykład: sudo apt-get update && sudo apt-get install -y tree
echo "Setup zakończony."
